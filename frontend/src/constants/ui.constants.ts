/** UI tuning: debounce, limits (match backend), default list size. */
export const PAGE_SIZE_DEFAULT = 50;
/** Gallery page size for pagination. Use a smaller value (e.g. 2) to test pagination with few videos. */
export const GALLERY_PAGE_SIZE = 9;

export const DEBOUNCE_MS_SEARCH = 300;

/** Max lengths (must match backend); used for validation and input maxLength */
export const TITLE_MAX_LENGTH = 70;
export const DESCRIPTION_MAX_LENGTH = 5000;
export const TAG_MIN_LENGTH = 3;
export const TAG_MAX_LENGTH = 20;
export const TAGS_MAX_COUNT = 15;
